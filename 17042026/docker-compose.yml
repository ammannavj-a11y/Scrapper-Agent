
version: '3'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - db

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: uvi
      POSTGRES_PASSWORD: uvi
      POSTGRES_DB: uvi
    ports:
      - "5432:5432"
